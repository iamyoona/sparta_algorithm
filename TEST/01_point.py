n = 6
arr = ['AF2', 'FA2', 'TA5', 'FT7', 'AF4', 'AT4']


"""class person(object):
    def __init__(self, name):
        self.name = name

    def __str__(self):
        return self.name

    def __repr__(self):
        return "'" + self.name + "'"""

temp_dict = {}
for info in arr:
    if info[0] in temp_dict.keys():
        temp_dict[info[0]] -int()
    temp_dict[info[0]] = -int(info[2])
    temp_dict[info[1]] = int(info[2])


if names[key] == min(names.values()) and names[key] < 0:
    answer.append(key)

